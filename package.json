{
  "name": "clearnexus-chrome-extension",
  "version": "0.0.1",
  "description": "",
  "main": "",
  "engines": {
    "node": "6.10"
  },
  "scripts": {
    "bower": "bower",
    "pulp": "pulp",
    "test": "pulp test",
    "build:dev": "export PURS_ENV=Dev && node ./app/main.js && pulp build",
    "build:stag" : "export PURS_ENV=Staging && node ./app/main.js && pulp build",
    "build:prod" : "export PURS_ENV=Prod && node ./app/main.js && pulp build"
  },
  "author": "Stackbuilders, Inc",
  "license": "ISC",
  "devDependencies": {
    "bower": "^1.8.0",
    "pulp": "^10.0.0",
    "purescript": "^0.10.6",
    "xhr2": "^0.1.4"
  }
}

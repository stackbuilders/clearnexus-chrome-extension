machine:
  node:
    version: 6.9.5
dependencies:
  cache_directories:
    - bower_components
    - node_modules
    - .pulp-cache
    - output

  pre:
    - npm install 

  override:
    - bower install
    - pulp build

test:
  override:
    - pulp test

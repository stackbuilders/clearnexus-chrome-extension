machine:
  node:
    version: 6.9.5
dependencies:
  cache_directories:
    - bower_components
    - node_modules
    - .pulp-cache

  pre:
    - npm install
    - bower install

  override:
    - pulp build

test:
  override:
    - pulp test

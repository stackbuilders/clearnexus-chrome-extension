machine:
  node:
    version: 6.9.5
dependencies:
  cache_directories:
    - bower_components
    - node_modules
    - .pulp-cache

  pre:
    - npm install
    - npm run bower install

  override:
    - npm run pulp build
